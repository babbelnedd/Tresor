<UserControl x:Class="Tresor.View.UserControls.PasswordTemplate" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mvvm="clr-namespace:Tresor.Framework.MVVM;assembly=Tresor.Framework" 
             x:Name="passwordTemplate" Focusable="False">
    <Grid Margin="0,2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Focusable="False">
        <TextBlock Text="{Binding Path=DataContext, ElementName=passwordTemplate}" 
                   mvvm:SingleEventCommand.CommandParameter="{Binding Path=DataContext, ElementName=passwordTemplate}" 
                   mvvm:SingleEventCommand.RoutedEventName="MouseLeftButtonUp" 
                   mvvm:SingleEventCommand.TheCommandToRun="{Binding DataContext.OpenTabCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}" />
    </Grid>
</UserControl>